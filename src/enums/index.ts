export enum Direction {
    Prev = 'prev',
    Next = 'next'
}

export enum QuestionType {
    MultipleChoice = 'multiple-choice',
    Text = 'text'
}

export enum Multiline {
    Yes = 'true',
    No = 'false'
}